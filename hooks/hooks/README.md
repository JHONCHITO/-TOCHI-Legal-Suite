# hooks/hooks

Carpeta heredada de estructura.

La logica real de hooks vive en `hooks/` y `lib/hooks/`. Este directorio se conserva
como espejo de compatibilidad para no romper referencias antiguas.
